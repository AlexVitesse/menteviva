# TODO Piloto Mente Viva — Ruta al MVP

Tareas ordenadas por urgencia para shipear el piloto. Convención: `[x]` hecho, `[ ]` pendiente, `[~]` en progreso.

**Definición de MVP:** un demo accesible vía URL pública (Cloudflare Tunnel sobre el server empresa), con login + diagnóstico + práctica + reporte funcionando para hasta ~100 usuarios concurrentes.

---

## 🔴 P0 — Ruta crítica al shipping (bloquean el demo)

**Meta: poder dar un link a un cliente esta semana.**

### 0.1 Backend listo para Postgres (~2h) — Plan 04
- [x] Auditar `app/db.py`, `services/user_repo.py`, `services/session_repo.py` (SQLite-isms identificados)
- [x] `poetry add "psycopg[binary,pool]"` + remove `aiosqlite`
- [x] Refactor `app/db.py` a psycopg async + `AsyncConnectionPool`, schema con `GENERATED BY DEFAULT AS IDENTITY`
- [x] Refactor `user_repo.py` y `session_repo.py`: `?` → `%s`, `cursor.lastrowid` → `RETURNING`
- [x] `DATABASE_URL` en `app/config.py` + `.env` (default Docker dev)
- [x] Fix Windows `WindowsSelectorEventLoopPolicy` para psycopg async → entry point `app/__main__.py`
- [x] Actualizar `start.bat` y `CLAUDE.md` a `python -m app`
- [x] Migración corre limpia contra Postgres dev (schema_version v0/v1/v2 OK)
- [x] Smoke `test_session_repo.py` y `inspect_users.py` contra Postgres dev OK
- [x] Smoke end-to-end via Chrome DevTools: registro nuevo de cero → Firebase + Postgres upsert + diagnóstico WS + TTS + diagnostic row guardado (ver `smoke-e2e-postgres-diagnostico.png`)
- [ ] Smoke con conversación real humana (no automatizada): hablar 3-5 turnos con Sofia + completar práctica con Roberto

### 0.2 Servir frontend desde uvicorn (~1h) — Plan 03
- [x] `app/main.py`: bloque `StaticFiles` + SPA fallback (`/assets/*`, `/vad/*`, `/avatars/*`, catch-all `index.html`)
- [x] `CachedStaticFiles` para `/assets/*` con `Cache-Control: max-age=31536000, immutable`; `index.html` con `no-cache`
- [x] `menteviva-frontend/.env.production` con `VITE_API_URL=` y `VITE_WS_URL=` vacíos
- [x] `useWebSocket.ts` ya construía `${proto}//${window.location.host}` cuando vacío — sin cambios
- [x] Build OK (`npm run build` produce `dist/{index.html, assets/, avatars/, vad/, vite.svg}`)
- [x] Smoke local validado: `/`, `/dashboard`, `/assets/index-*.js`, `/avatars/roberto.png`, `/vad/*.wasm`, `/api/avatars`, `/api/zzz_404` todos responden correctamente desde un solo origen (`:8000`)
- [x] `deploy.sh` creado en root: pull + poetry install + migrator + npm build + systemctl restart + smoke /health
- [ ] Smoke E2E browser sobre `:8000` (sin Vite): registro + diagnóstico + práctica end-to-end

### 0.3 Repo en GitHub (~30 min) — Plan 01
- [ ] Crear repo `menteviva` privado en GitHub
- [ ] `git remote add origin` + `git push -u origin main` + `git push -u origin dev`
- [ ] Branch protection en `main`: require PR, 1 approval, no force push, no delete
- [ ] Default branch = `dev`

### 0.4 Setup Neon prod (~30 min)
- [ ] Crear Neon project `menteviva-piloto` (región us-east-2, Pg 16)
- [ ] Copiar Pooled connection string
- [ ] Correr migración contra Neon desde local
- [ ] Smoke: `psql "$NEON_URL" -c '\d'` muestra todas las tablas

### 0.5 Deploy en servidor empresa (~2-3h)
- [ ] Aprovisionar VM / acceso al server
- [ ] Instalar Python 3.11+, Node 20, Poetry, postgres-client, cloudflared
- [ ] Clonar repo, `.env` con secrets prod (Firebase, Groq, ElevenLabs, `DATABASE_URL`=Neon)
- [ ] `deploy.sh` con `git pull + poetry install + npm ci && npm run build + systemctl restart`
- [ ] systemd unit `menteviva-backend.service` (uvicorn 4 workers)
- [ ] systemd unit `cloudflared.service` apuntando al subdominio piloto
- [ ] Añadir subdominio Cloudflare a authorized domains en Firebase Auth
- [ ] Smoke full-flow desde laptop sobre la URL pública

### 0.6 Validación pre-demo (~1h)
- [ ] Fix Sofía Fase 5: hoy anuncia "déjame hacerte una pregunta final" y emite `[CIERRE]` sin la pregunta — task #28
- [ ] Validar Sofía y María bajo `gpt-oss-20b` (adaptar `test_roberto_long_session.py`)
- [ ] Validar fallback automático a `llama-3.1-8b-instant` cuando gpt-oss tira `tool_choice` glitch

---

## 🟠 P1 — Crítico tras shipping (primeros días con tráfico)

**Meta: que el demo aguante 100 usuarios sin reventar costos o caer.**

### 1.1 TTS swap a OpenAI (~1.5h) — Plan 02
- [ ] `poetry add openai` (si no está)
- [ ] `TTS_PROVIDER` + `OPENAI_API_KEY` en `.env` y `Settings`
- [ ] Refactor `services/edge_tts.py` con dispatcher OpenAI/ElevenLabs
- [ ] Mapeo voces: sofia→`alloy`, roberto→`onyx`, maria→`nova`
- [ ] A/B con `scripts/test_tts.py` (calidad español, latencia)
- [ ] Sesión de 5 min en `/diagnostico` y `/simulation` para validar
- [ ] Default `TTS_PROVIDER=openai` con fallback EL automático si error

**Justificación:** $0.015/1k chars vs $0.066-0.10 → ~6× más barato. A 100 usuarios concurrentes la diferencia es material.

### 1.2 Observabilidad mínima
- [ ] logrotate config con `copytruncate` para `menteviva-backend/logs/*.log`
- [ ] Verificar que `gpt-oss-20b → llama-3.1-8b-instant` fallback se loguea claro
- [ ] Backup `pg_dump` manual semanal a S3 / disco local del server
- [ ] Bug bash con 2-3 usuarios reales — task #29

### 1.3 Headers de cache (opcional, mejora UX)
- [ ] `CachedStaticFiles` con `Cache-Control: max-age=31536000, immutable` para `/assets/*`
- [ ] `Cache-Control: no-cache` para `index.html` (SPA fallback)

---

## 🟡 P2 — Calidad de producto (siguiente sprint, semana 2-3)

**Meta: que el demo se sienta pulido y diferenciado.**

### 2.1 Loop adaptativo — motor (~5h)
> Roadmap lo llama "bloqueador piloto B2B". Para demo de **1 sesión** no se nota; a partir de sesión 3 sí.

- [ ] Alinear shape `ConversationAnalysis` con `Diagnostico`
- [ ] `mergeAnalysisIntoProfile()` backend + wire frontend tras `session_end`
- [ ] Reglas: confirmar `strength` con 2+ sesiones, bajar `gap` con score>70, reemplazar `blind_spot`, rotar `recommended_next_scenario`
- [ ] **Reconciliar catálogo de skills**: diagnóstico usa 10 habilidades, `SKILLS_BY_SCENARIO` usa 5 hardcoded — usuario ve "comunicación" en perfil pero "manejo_objeciones" en reporte

### 2.2 Loop adaptativo — UI (~3h)
- [ ] Barras de progreso por habilidad (0-100) en Report + `/mi-plan`
- [ ] Strengths confirmadas con check + "respaldada por N sesiones"
- [ ] Áreas de foco con progreso desde valor inicial del diagnóstico
- [ ] "Tu siguiente reto" como tarjeta destacada en dashboard
- [ ] Indicador de constancia discreto ("12 sesiones este mes")

### 2.3 Contenido faltante
- [ ] Sofía custom Ready Player Me (hoy es `brunette.glb` genérico)
- [ ] Fix Sofía Fase 5 a nivel prompt (si no se resolvió en P0)
- [ ] María refactor a "Celeste" del CAT-01 científico
- [ ] María niveles Intermedio/Avanzado (Roberto ya los tiene)

---

## 🟢 P3 — Expansión (mes 2+)

### 3.1 Carlos completo (CAT-03 Entrevistas)
- [ ] `system_prompt` propio
- [ ] Visual PNG/3D propio (hoy comparte voz de Roberto)
- [ ] Rubrica en `SKILLS_BY_SCENARIO` / `KPIS_BY_SCENARIO`

### 3.2 Usuario power-features
- [ ] Re-diagnóstico desde `/mi-plan` (botón + flujo cada N sesiones)
- [ ] Edición de registro desde `/mi-plan`
- [ ] Histórico de sesiones + curva de evolución del perfil en `/mi-plan`
- [ ] SEO básico: Open Graph tags en `index.html` (twitter card, og:image)

### 3.3 Producción ready
- [ ] `.github/workflows/ci.yml`: typecheck frontend + import backend en PRs
- [ ] DB branching para PRs vía `neonctl` (preview deploys)
- [ ] (Opcional) Scrub Groq key del commit `c077aa8` con `git filter-repo` (key ya revocada, decisión: dejar historia)

---

## 🔵 P4 — Post-piloto / B2B real

- [ ] Multi-tenant: organización → equipos → usuarios
- [ ] Pantallas P-13/P-14 manager: dashboard agregado + ficha empleado
- [ ] Métricas HR: cobertura, evolución por habilidad agregada, brechas grupo
- [ ] Auth con roles (manager vs colaborador)
- [ ] Lip-sync con visemas reales (timestamps ElevenLabs o Whisper word-level)
- [ ] Catálogo CAT-02 a CAT-08 del spec original
- [ ] Cleanup post-piloto: `poetry remove elevenlabs`, renombrar `edge_tts.py` → `tts.py`, eliminar rama SQLite

---

## ✅ Ya hecho (referencia)

- [x] Ramas `main` y `dev` locales
- [x] `docker-compose.dev.yml` con Postgres 16
- [x] Postgres dev levantado y healthy (`menteviva-pg-dev`)
- [x] Plans escritos: 01 (remote), 02 (TTS), 03 (StaticFiles), 04 (Postgres)
- [x] Roberto-Cóndor 3 niveles + 6 KPIs
- [x] Dashboard `/mi-plan` desde SQLite
- [x] Firebase Auth backend + frontend
- [x] Avatares 3D Roberto/María/Sofía (lip-sync por amplitud)
- [x] Idle animation oficial RPM (brazos abajo, no T-pose)
- [x] Diagnóstico estilo Zoom + mute
- [x] UserMenu con foto Google + dropdown

---

## Resumen ejecutivo

| Bloque | Esfuerzo | Cuándo |
|---|---|---|
| **P0 ruta crítica** | ~7-8h (1-2 días de trabajo enfocado) | YA — antes de shipear |
| P1 estabilización | ~2-3h | Primera semana con tráfico |
| P2 calidad producto | ~10-12h | Semana 2-3 |
| P3 expansión | depende del scope | Mes 2 |
| P4 B2B real | semanas | Mes 3+ |

**Próxima acción concreta:** ejecutar 0.1 (Postgres-ready). El Docker dev ya está corriendo, solo falta el refactor del backend para usarlo.
